# Published sessions

This page is replaced during the documentation build with a deterministic index
of reviewed public traces. Run `./scripts/build-docs.sh` to generate the current
index, complete transcripts, normalized events, and raw archive downloads.

The immutable receipts live in `traces/manifest.jsonl`.
