---
type: session
id: 20260702-073627-029329000
runtime_id: abbc55cf-de36-4cf2-88be-058f7f1ba679
continuity_uid: abbc55cf-de36-4cf2-88be-058f7f1ba679
work_spec: mct-typed-wit-runtime-parity
title: MCT kernel invariant docs
status: active
llm: pi
interface: pi
created: 2026-07-02T11:36:27.029373+00:00
updated: 2026-07-05T11:57:49.582303+00:00
start_timestamp: 1782992187029
source_log: /Users/nicabar/.pi/agent/sessions/--Users-nicabar-Projects-Patina-patina-mct--/2026-07-02T10-29-08-375Z_019f2260-0e97-71ff-86c8-dc68bc02e1b9.jsonl
participants:
- id: pi-1616
  role: interface
  interface: pi
  interface_name: pi
  display_name: nicabar
  joined_at: 2026-07-02T11:36:27.029373+00:00
interfaces:
- pi
git:
  project_uid: puid_9105e7eadfa14c89b16e311ee5611da7
  branch: none
  starting_commit: none
  start_tag: session-20260702-073627-029329000-pi-start
---

## Previous Session Context
<!-- AI: Summarize the last session from last-session.md -->

## Goals
- [x] MCT kernel invariant docs

## Activity Log
### 07:36 - Session Start
Session initialized with goal: pi session
Working on branch: none
Tagged as: session-20260702-073627-029329000-pi-start

### 19:40 - Update (covering since 07:36)

**PI Distilled Activity:**
- Source log: `/Users/nicabar/.pi/agent/sessions/--Users-nicabar-Projects-Patina-patina-mct--/2026-07-02T10-29-08-375Z_019f2260-0e97-71ff-86c8-dc68bc02e1b9.jsonl`
- Span: no new PI log events since 2026-07-02T11:36:27.029373+00:00

**Work completed:**
- Verified HEAD was `[[commit-be6d678]]` before starting P5b, then added the missing Task P5b text to `layer/surface/build/audit-remediation/PHASE2.md` in `[[commit-a63e7f8]]` before implementation work.
- Replaced boilerplate rustdoc in `mct-kernel` with invariant-focused documentation across authority modules:
  - `crates/mct-kernel/src/call/mod.rs` in `[[commit-19e5adc]]` documented call records, JSON edge encode/decode, payload-size invariants, and `evaluate_call_protocol` fail-closed behavior.
  - `crates/mct-kernel/src/error.rs` in `[[commit-05d02b1]]` and `[[commit-e3052e7]]` documented typed validation/JSON errors and clarified error-field purpose.
  - `crates/mct-kernel/src/peer/mod.rs` in `[[commit-4cc2e7b]]` documented hello admission, peer binding expiry/revision/ALPN invariants, and safe response projection.
  - `crates/mct-kernel/src/route.rs` in `[[commit-fe72dc5]]` documented route candidate filtering, no-route defaults, and execution-time revalidation tokens.
  - `crates/mct-kernel/src/toy.rs` in `[[commit-b918d42]]` documented canonical toy contracts, grant matching, time windows, revision checks, and authorized toy-call tokens.
  - `crates/mct-kernel/src/child.rs` in `[[commit-353a912]]` documented artifact/approval/assignment/instance authority and child invocation token invariants.
  - `crates/mct-kernel/src/observation.rs` in `[[commit-6f9ba41]]` documented observation disclosure boundaries, source planes, visibility, and decision-to-ledger projections.
  - `crates/mct-kernel/src/lib.rs` in `[[commit-0b41d47]]` documented module-level authority boundaries and checked P5b complete in `layer/surface/build/audit-remediation/PHASE2.md`.
- Verified `cargo doc -p mct-kernel --no-deps` warning-free and removed all boilerplate patterns found by grep (`Domain record`, `Field`, `Closed domain enum`, `Public`, `Evaluates`, `Executes`) from `crates/mct-kernel/src`.

**Discussion context:**
- Work followed [[read-code-before-write]], [[fail-closed]], and [[kernel-decides-adapters-perform]] principles from `AGENTS.md`, `layer/core/dependable-rust.md`, and `layer/core/what-is-mct.md`.
- The task was docs-only: no runtime behavior changes, no doctests, no `#[allow(missing_docs)]`, and one commit per kernel module or narrow doc boundary.
- The documentation pass described actual implementation behavior by reading each authority evaluator before documenting it.

**Key decisions:**
- Kept denial semantics explicit in docs: absence, staleness, mismatch, revocation, expiry, or missing authority is a typed denied/no-route evaluation, not an error.
- Documented authorization tokens (`AuthorizedChildInvocation`, `AuthorizedToyCall`, `AuthorizedRouteExecution`) as executable capabilities minted only by successful revalidation/evaluation.
- Treated safe messages and observation visibility as disclosure boundaries: caller-safe projections are separate from audit reasons and detail references.

**Challenges faced:**
- Full validation intermittently hit a pre-existing daemon test flake in `tests::fake_echo_slice_records_trace_and_result` around observation ledger writer locking. The same full suite passed on rerun without code changes.
- The task had a large documentation surface; grep-based cleanup was used to catch leftover template comments while keeping changes docs-only.

**Patterns observed:**
- A reliable pattern for authority docs is: name the question answered, list the authority facts consumed, then state exactly what fails closed and what token/decision is produced.
- Grep for boilerplate phrases after each module makes doc quality remediation measurable without changing behavior.

**Git Activity:**
- Commits this session: 10 docs commits from `[[commit-a63e7f8]]` through `[[commit-0b41d47]]`.
- Files changed: 15
- Last commit: 61 minutes ago
- Git range: `session-20260702-073627-029329000-pi-start..HEAD`


### 19:51 - Update (covering since 19:40)

**PI Distilled Activity:**
- Source log: `/Users/nicabar/.pi/agent/sessions/--Users-nicabar-Projects-Patina-patina-mct--/2026-07-02T10-29-08-375Z_019f2260-0e97-71ff-86c8-dc68bc02e1b9.jsonl`
- Span: no new PI log events since 2026-07-02T23:40:09.042826+00:00

**Work completed:**
- Renamed this durable session from default title `pi session` to `MCT kernel invariant docs` after confirmation.
- Updated the session artifact to capture the P5b documentation work and preserve the Git range boundary `session-20260702-073627-029329000-pi-start..HEAD`.

**Discussion context:**
- The first-update naming hook recommended a task-specific title because the session title was still a default interface title.

**Key decisions:**
- Use the concise title `MCT kernel invariant docs` because the durable work centered on replacing boilerplate kernel docs with authority invariant documentation.

**Challenges faced:**
- The session wrapper reported zero commits in its own JSON because the PI source log had no new events, so the artifact narrative records the actual committed work explicitly with commit wikilinks.

**Patterns observed:**
- For long-running sessions, the durable artifact needs both wrapper metadata and a human-authored Git-aware summary so `patina scrape` can trace commits and source files.

**Git Activity:**
- Commits this session: 0 reported by wrapper; artifact narrative links the relevant commits manually.
- Files changed: 15
- Last commit: 72 minutes ago
- Git range: `session-20260702-073627-029329000-pi-start..HEAD`


### 07:57 - Update (covering since 19:51)

**PI Distilled Activity:**
- Source log: `/Users/nicabar/.pi/agent/sessions/--Users-nicabar-Projects-Patina-patina-mct--/2026-07-02T10-29-08-375Z_019f2260-0e97-71ff-86c8-dc68bc02e1b9.jsonl`
- Span: no new PI log events since 2026-07-02T23:51:33.367390+00:00

**Work completed:**
- Closed out the integrated Patina SDK MCT child-authoring track in `~/Projects/Sandbox/AI/RUST/patina` before returning to this repo. The track's authored commits were fixed from the fixture identity to `NicabarNimble <nicabar@gmail.com>` with an empty tree diff, then a follow-up canonical WIT enrollment was added in `[[commit-c57f66bf]]` after the integrated repo's pre-push hook rejected SDK-only MCT WIT copies.
- Landed the integrated Patina branch after validation. Relevant cross-repo commits: `[[commit-e37e1d43]]`, `[[commit-55bc9418]]`, `[[commit-26d5a8dd]]`, and `[[commit-c57f66bf]]`.
- Started MCT Phase 5 payload data plane in this repo. Verified branch `patina` was in sync with `origin/patina`, HEAD was `[[commit-181a98a]]`, `origin/main` carried `[[commit-d6b8da5]]`, and the only pre-existing untracked file was `brew-noncore-report.html`.
- Completed D0 in `[[commit-7f47302]]`: ticked the product ROADMAP backlog item for Child SDK / packaging tooling in `layer/surface/build/product/ROADMAP.md` and added `layer/surface/build/feat/payload-data-plane/TASKS.md` with the full phase prompt, checklist, and flake log.
- Completed D1 in `[[commit-e12257e]]`: wrote [SPEC.md](layer/surface/build/feat/payload-data-plane/SPEC.md) for bounded inline payload bytes over `mct/call/0` and checked off D1 in `layer/surface/build/feat/payload-data-plane/TASKS.md`.
- Re-read the requested code/design surfaces before the SPEC: `crates/mct-kernel/src/call/mod.rs`, `crates/mct-iroh/src/serve.rs`, `crates/mct-daemon/src/main.rs`, `crates/mct-daemon/src/process.rs`, `crates/mct-daemon/src/wasm.rs`, and `crates/mct-daemon/src/wit_values.rs`.
- Ran the required validation after both MCT commits: `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `./scripts/ci-tier0.sh`; all passed.

**Discussion context:**
- The SDK closeout followed the existing integrated Patina hook convention rather than bypassing it: top-level `wit/<world>/` is canonical and `sdk/patina-sdk/wit/<world>/` is a byte-identical mirror. That corrected the original SDK-track framing without altering legacy WIT content.
- The Phase 5 MCT work followed [[read-code-before-write]], [[kernel-decides-adapters-perform]], [[fail-closed]], and [[spec-driven-design]] discipline. The operator explicitly required D1 as a gate before D2, so implementation stopped after the SPEC commit.
- Current MCT git range for this durable session remains `session-20260702-073627-029329000-pi-start..HEAD`; the new MCT phase commits in that range are `[[commit-7f47302]]` and `[[commit-e12257e]]`.

**Key decisions:**
- Phase 5 SPEC chose base64 inline payload fields inside the existing JSON envelope over a length-prefixed binary section because 32 KiB caps keep overhead acceptable and preserve a simple debuggable 0.x protocol edge.
- SPEC fixed named bounds: 32 KiB request inline payload cap, 32 KiB result inline payload cap, and 96 KiB request/reply frame read budget.
- SPEC requires `InlinePayload` to carry a `blake3_digest_hex` declaration. Adapters decode and hash bytes; the pure kernel compares declared versus observed size/digest facts.
- SPEC maps WIT request payload bytes to `application/json` call arguments through `wit_values`, process request payload bytes to stdin, WIT result JSON to result bytes, and process stdout to result bytes.
- SPEC keeps payload bytes out of observations and the JSONL ledger; audit records contain size, digest, classification, and typed outcome only.

**Challenges faced:**
- Integrated Patina push initially failed because the pre-push WIT consistency hook detected SDK MCT deps without canonical `wit/mct/` counterparts. The fix was to enroll `mct` fully in the repo's canonical WIT scheme and commit that separately.
- The MCT session wrapper reported zero commits because its PI source log had no new events since the prior update, so this artifact records the actual Git-aware progress manually with commit wikilinks.
- Phase 5 has a high-risk invariant surface: adding payload bytes must not weaken authority, let unbounded reads reappear, or put business payloads into the ledger. The SPEC resolves these before code work begins.

**Patterns observed:**
- Hook-enforced repository conventions are design signals, not obstacles; the integrated SDK WIT failure exposed the correct canonical/mirror scheme.
- A useful payload-data-plane shape is: bounded read, decode, verify integrity, then authority, then effect. This sequencing keeps malformed data out of the authority/effect path.
- For cross-repo work, explicit commit wikilinks and path references are needed because a single session artifact may span MCT and integrated Patina repositories.

**Git Activity:**
- Commits this session reported by wrapper: 0; actual new MCT commits since the prior manual update: `[[commit-7f47302]]`, `[[commit-e12257e]]`.
- Files changed in committed MCT work: `layer/surface/build/product/ROADMAP.md`, `layer/surface/build/feat/payload-data-plane/TASKS.md`, and `layer/surface/build/feat/payload-data-plane/SPEC.md`.
- Working tree after this update artifact edit: only this session file is modified; `brew-noncore-report.html` remains the pre-existing untracked file.
- Git range: `session-20260702-073627-029329000-pi-start..HEAD`


## Decisions

## Evidence

## Handoff

## Outcome
