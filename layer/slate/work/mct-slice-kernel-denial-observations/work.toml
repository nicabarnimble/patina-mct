id = "mct-slice-kernel-denial-observations"
title = "Map kernel denial evaluations to observations"
kind = "build"
status = "active"
execution_granularity = "slice"
human_request = "Standardize how kernel hello/call denial evaluations become observation-grade MctObservation facts."
allium_anchors = ["MctObservation", "MctHelloAdmissionEvaluation", "MctCallProtocolEvaluation", "MctRuntimeShape"]
user_alignment = "User wants `MctObservation` as canonical runtime truth and denied authority paths to be explicit facts."
belief_refs = ["mct-hello-precedes-protected-peer-effects", "mct-call-protocol-wraps-semantic-call"]
core_refs = ["layer/core/safety-boundaries.md", "layer/core/oxidized-knowledge.md", "layer/core/spec-driven-design.md"]
parent_slates = ["mct-kernel-crate", "mct-observation-log", "mct-node-identity-security"]
code_targets = ["crates/mct-kernel/src/observation.rs"]
implementation_ready = true
runtime_shape_role = "Kernel decisions return observation-grade facts for denied authority paths."
design_style = "Pure domain mapping with typed outcomes; no ledger/storage side effects."
first_test_to_write = "kernel_denial_evaluations_become_observations"
exit_criteria = ["Denied hello evaluation maps to PeerRejected observation.", "Denied call protocol evaluation maps to CallDenied observation.", "Observations carry trace, decision, safe message, and denied outcome."]
non_goals = ["No ledger writes.", "No adapter observations.", "No full coverage of every future authority decision."]
implementation_plan = [
    "[ ] Add pure observation builders for hello and call protocol evaluations.",
    "[ ] Map denied outcomes to denied observation kinds and outcomes.",
    "[ ] Add kernel tests for denial observation facts.",
]
proof_plan = [
    "[ ] `cargo test -p mct-kernel kernel_denial_evaluations_become_observations` passes.",
    "[ ] Test proves denied hello maps to `PeerRejected`.",
    "[ ] Test proves denied call maps to `CallDenied`.",
    "[ ] `cargo test --workspace` passes.",
    "[ ] `allium check layer/allium` passes.",
]
closure_evidence = []
blocked_by = ["mct-slice-hello-admission", "mct-slice-call-protocol-evaluation"]
created_at = "1780276400"
updated_at = "1780276400"
