id = "mct-node-identity-security"
title = "Build node identity and security model"
kind = "build"
status = "blocked"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Define and implement MCT node identity, peer admission, caller authentication, child/component authorization, and WIT interface exposure policy across local and Iroh boundaries."
allium_anchors = ["MctPeerBinding", "MctPeerAdmissionDecision", "MctIrohPeerBindingAuthority", "MctIrohProtocolPolicy", "MctIrohProtocolLayer", "MctHelloAdmissionEvaluation", "MctHelloProtocol", "MctRuntimeShape"]
user_alignment = "User wants a real Iroh substrate and explicit authority without over-engineering; node/security policy should be first-class rather than buried inside transport code. User agreed that Iroh EndpointId is transport identity only and MCT needs explicit observed binding to Node/Vision/admission/policy authority."
belief_refs = ["iroh-endpointid-is-transport-identity", "iroh-provides-connectivity-not-authority", "mct-hello-precedes-protected-peer-effects"]
core_refs = ["layer/core/mct-build-boundaries.md", "layer/core/safety-boundaries.md", "layer/core/adapter-pattern.md"]
runtime_shape_role = "Identity/admission authority lane: binds transport identities and local identities to explicit MCT Node/Vision/policy facts."
design_style = "Authority by explicit domain facts, not ambient transport/session identity."
implementation_ready_requirements = ["Every slice states which identity fact grants or denies authority.", "Proof gates include revoked/expired/stale/unknown cases.", "Endpoint discovery/reachability remains evidence, not admission."]
slice_refs = ["mct-slice-kernel-domain-records", "mct-slice-hello-admission", "mct-slice-local-iroh-roundtrip", "mct-slice-runtime-shape-anchors"]
partial_evidence = ["[x] `MctPeerBinding` and hello admission encode the EndpointId-to-MCT-authority boundary.", "[x] Tests deny unknown, mismatched, revoked, stale, and unsupported hello requests.", "[ ] Persistent identity, key material, revocation/expiry observations, and administrative binding lifecycle remain open."]
next_slice_candidates = ["mct-slice-node-identity-persistence", "mct-slice-peer-binding-revocation-expiry"]
proof_plan = [
    "[ ] Unknown or untrusted peer/caller requests are denied with typed reasons and kernel observations.",
    "[ ] Iroh EndpointId alone never authorizes an MCT peer action; an active MctPeerBinding is required for peer admission.",
    "[ ] Peer binding creation, hello receipt, protocol negotiation, admission, rejection, revocation, expiry, and stale-policy denial are represented as observations.",
    "[ ] Authorized node/child/interface identities can be represented without raw strings at the authority boundary.",
    "[ ] Iroh, local control, JVM bridge, and process/WASM adapters share policy decisions instead of duplicating security logic.",
]
closure_evidence = []
blocked_by = [
    "mct-allium-foundation",
    "mct-kernel-crate",
    "mct-storage-core",
]
implementation_plan = [
    "[ ] Define Mother/Node/User/Interface/Child/Component caller identities and their trust roots.",
    "[ ] Define MctPeerBinding as the authority record binding Iroh EndpointId to MCT Node, Vision scope, allowed ALPNs, issuer, policy revision, and time bounds.",
    "[ ] Define mct/hello/0 peer admission and local caller authentication rules for UDS/HTTP/Iroh/JVM/process boundaries.",
    "[ ] Define component/WIT-interface exposure policy and how grants are derived from manifests and assignments.",
    "[ ] Implement fail-closed authorization checks and observations that other adapters call into rather than reimplementing policy.",
    "[ ] Ensure Iroh endpoint hooks and relay access control call into or mirror MCT peer binding decisions rather than becoming a separate authority model.",
]
created_at = "1780070076"
updated_at = "1780264533"
block_reason = "Epic remains open; hello admission covers first peer binding boundary, but persistent identity/security lifecycle still requires smaller slice Slates."
