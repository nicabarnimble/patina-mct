id = "mct-kernel-crate"
title = "Build mct-kernel crate authority core"
kind = "build"
status = "blocked"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Implement the `mct-kernel` crate as the small, typed, performance-sensitive authority core for child identity, lifecycle, routing, grants, errors, and kernel observations."
allium_anchors = []
user_alignment = "User mostly agrees with `mct-kernel` but wants the build to stay clean, performant, and not over-engineered."
belief_refs = []
core_refs = ["layer/core/mct-build-boundaries.md", "layer/core/dependable-rust.md", "layer/core/spec-driven-design.md", "layer/core/safety-boundaries.md"]
slice_refs = ["mct-slice-workspace-skeleton", "mct-slice-kernel-domain-records", "mct-slice-hello-admission", "mct-slice-call-protocol-evaluation", "mct-slice-fake-daemon-echo"]
partial_evidence = ["[x] Kernel crate exists with typed IDs and domain records.", "[x] Pure hello and call protocol authority evaluations exist.", "[ ] Child lifecycle, toy grants, routing decisions, and typed kernel errors remain open."]
next_slice_candidates = ["mct-slice-kernel-denial-observations", "mct-slice-kernel-routing-decision-records", "mct-slice-toy-grant-authorized-call"]
proof_plan = [
    "[ ] `cargo test` passes for the kernel crate.",
    "[ ] Public API exposes typed domain concepts and not transport/runtime/storage implementation types.",
    "[ ] Denied/unknown/not-ready paths fail closed with typed errors and kernel observations.",
]
closure_evidence = []
blocked_by = [
    "mother-kernel-design",
    "mct-call-envelope",
    "mct-allium-foundation",
]
implementation_plan = [
    "[ ] Create the Rust crate/module boundary for `mct-kernel` with narrow public API and private internals.",
    "[ ] Implement typed IDs, lifecycle state machine, operation references, grant decisions, routing decisions, and typed errors.",
    "[ ] Keep HTTP, Iroh, JVM, Wasmtime, SQLite row types, and Belief/session logic out of the public kernel API.",
    "[ ] Add focused unit tests for authority decisions and lifecycle transitions.",
]
created_at = "1780068471"
updated_at = "1780262310"
block_reason = "Epic remains open; completed child slices cover crate skeleton, domain records, hello admission, and call protocol evaluation, but lifecycle/routing/grants/errors still require smaller slice Slates."
