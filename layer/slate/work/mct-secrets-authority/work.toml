id = "mct-secrets-authority"
title = "Build MCT secrets authority"
kind = "build"
status = "active"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Implement controlled secrets authority for children, bridges, launchers, and toys without exposing secrets ambiently."
allium_anchors = []
user_alignment = "Secrets are MCT control-plane infrastructure and must be explicit, authorized, and observable."
belief_refs = []
core_refs = ["layer/core/safety-boundaries.md", "layer/core/adapter-pattern.md", "layer/core/mct-build-boundaries.md"]
proof_plan = [
    "[ ] Unauthorized secret requests are denied and observed without leaking secret material.",
    "[ ] Authorized secret use is scoped to child/bridge/toy identity.",
    "[ ] Logs/observations redact secret values.",
]
closure_evidence = []
blocked_by = []
implementation_plan = [
    "[ ] Define secret identity, scope, grant requirements, and redaction rules.",
    "[ ] Choose local backing store/key management for first pass based on current Mother reference.",
    "[ ] Implement get/cache/lock or equivalent minimal control-plane operations.",
    "[ ] Emit kernel observations for authorization decisions and adapter observations for backend failures.",
]
created_at = "1780068545"
updated_at = "1780262310"
unblock_reason = "Node identity/security is complete. Epic is ready for explicit secrets authority slices."
