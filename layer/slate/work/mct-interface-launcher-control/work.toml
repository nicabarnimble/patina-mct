id = "mct-interface-launcher-control"
title = "Build interface launcher and control API"
kind = "build"
status = "paused"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Rebuild the launcher behavior currently surfaced as `patina ai` as an MCT-owned interface/HITL launcher and control-plane handshake."
allium_anchors = []
user_alignment = "User explicitly said MCT will own the launcher behavior currently in `patina ai`."
belief_refs = []
core_refs = ["layer/core/safety-boundaries.md", "layer/core/adapter-pattern.md", "layer/core/mct-build-boundaries.md"]
proof_plan = [
    "[x] Runtime replacement v0 explicitly excludes interface launcher behavior from MCT core.",
    "[x] Runbook records that Belief/scry/assay/session/interface orchestration remains above MCT.",
    "[x] Current `patina ai` behavior is classified as defer/bridge for follow-on interface-layer work, not a Mother runtime blocker.",
]
closure_evidence = [
    "Reclassified out of the MCT runtime replacement v0 boundary in `layer/surface/build/product/MOTHER-REPLACEMENT-RUNBOOK.md`.",
    "Secrets authority is complete, so this is no longer blocked by secrets; it is deferred as interface orchestration outside the runtime core.",
]
blocked_by = []
implementation_plan = [
    "[x] Inventory current launcher ownership at the product-boundary level for v0.",
    "[x] Define v0 boundary: launcher/session/interface orchestration is above MCT runtime core.",
    "[x] Defer launch/attach/handshake/control basics to a follow-on interface-layer slice.",
    "[x] Defer skill projection/session artifact capture to follow-on work.",
]
created_at = "1780068558"
updated_at = "1780262310"
defer_reason = "Not a Mother runtime replacement blocker for v0; implement as a follow-on interface-layer slice above MCT."
