id = "mct-slice-workspace-skeleton"
title = "Implement Rust workspace skeleton"
kind = "build"
status = "complete"
human_request = "Create the initial Rust workspace and crate boundaries for the clean MCT build."
execution_granularity = "slice"
allium_anchors = []
user_alignment = "User asked for implementation slices 0-6 with small scalpel commits, anchored in Allium/Slate/Core rather than a final shotgun change."
belief_refs = []
core_refs = ["layer/core/mct-build-boundaries.md", "layer/core/spec-driven-design.md", "layer/core/dependable-rust.md", "layer/core/unix-philosophy.md"]
parent_slates = ["mct-build-roadmap", "mct-kernel-crate", "mct-observation-log", "mct-iroh-substrate", "mct-daemon-control-plane"]
code_targets = ["Cargo.toml", "Cargo.lock", ".github/workflows/ci.yml", ".githooks/pre-commit", "scripts/ci-tier0.sh", "crates/mct-kernel/", "crates/mct-observation/", "crates/mct-iroh/", "crates/mct-daemon/"]
non_goals = ["No runtime authority implementation beyond crate/version smoke tests.", "No Iroh endpoint effects.", "No WASM/WASI/WIT child runtime."]
implementation_plan = ["[x] Create the workspace Cargo.toml and member crates.", "[x] Add crate-local lib/main skeletons with version smoke tests.", "[x] Wire CI/pre-commit to run Allium validation and workspace tests."]
proof_plan = ["[x] Workspace includes mct-kernel, mct-observation, mct-iroh, and mct-daemon crates.", "[x] CI/pre-commit path runs `allium check layer/allium` and `cargo test --workspace`.", "[x] `cargo test --workspace` passes.", "[x] `allium check layer/allium` passes."]
closure_evidence = ["[x] Commit `cd6244d build: add rust workspace skeleton`.", "[x] Later full validation also passed in `./scripts/ci-tier0.sh` after commit `a35b598`."]
proof_commands = ["cargo test --workspace", "allium check layer/allium", "./scripts/ci-tier0.sh"]
blocked_by = []
created_at = "1780262176"
updated_at = "1780262176"
closed_at = "1780262176"
