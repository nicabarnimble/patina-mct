id = "mct-slice-hello-admission"
title = "Implement pure mct/hello/0 admission evaluation"
kind = "build"
status = "complete"
human_request = "Add pure kernel evaluation for `mct/hello/0` peer admission before protected peer effects."
execution_granularity = "slice"
allium_anchors = ["MctPeerBinding", "MctPeerAdmissionDecision", "MctHelloRequest", "MctHelloAdmissionEvaluation", "MctHelloResponse", "MctHelloProtocol"]
user_alignment = "User locked the rule that Iroh EndpointId is transport identity only and MCT requires explicit observed peer binding/admission."
belief_refs = ["iroh-endpointid-is-transport-identity", "mct-hello-precedes-protected-peer-effects", "iroh-provides-connectivity-not-authority"]
core_refs = ["layer/core/safety-boundaries.md", "layer/core/spec-driven-design.md", "layer/core/mct-build-boundaries.md"]
parent_slates = ["mct-node-identity-security", "mct-iroh-substrate", "mct-kernel-crate", "mct-observation-log"]
code_targets = ["crates/mct-kernel/src/peer/mod.rs", "crates/mct-kernel/src/peer/internal.rs"]
non_goals = ["No network accept loop.", "No persistent peer store.", "No signature verification implementation beyond representing signature refs."]
implementation_plan = ["[x] Move peer internals behind a black-box module boundary.", "[x] Implement `evaluate_hello` using active binding, endpoint, Vision, ALPN, policy revision, and version checks.", "[x] Return caller-safe hello responses.", "[x] Add denial/admission unit tests."]
proof_plan = ["[x] Unknown endpoint is denied.", "[x] Endpoint mismatch is denied before binding lookup.", "[x] Revoked binding is denied with safe message.", "[x] Stale policy revision is denied.", "[x] Unsupported major version requests upgrade.", "[x] Active binding admits only intersection of requested, policy, and binding ALPNs.", "[x] `cargo test -p mct-kernel` passes."]
closure_evidence = ["[x] Commit `62a88a9 feat(kernel): evaluate hello peer admission`.", "[x] Later full validation also passed in `./scripts/ci-tier0.sh` after commit `a35b598`."]
proof_commands = ["cargo test -p mct-kernel", "allium check layer/allium", "./scripts/ci-tier0.sh"]
blocked_by = []
created_at = "1780262176"
updated_at = "1780262176"
closed_at = "1780262176"
