id = "mct-release-hardening"
title = "Harden MCT build for release"
kind = "build"
status = "paused"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Add integration tests, performance checks, security review, packaging, docs, and release criteria for the clean MCT build."
allium_anchors = []
user_alignment = "The clean MCT repo must be dependable, performant at the core, and built in reviewable slices before SDK/Belief follow-on work."
belief_refs = []
core_refs = ["layer/core/safety-boundaries.md", "layer/core/spec-driven-design.md", "layer/core/mct-build-boundaries.md"]
proof_plan = [
    "[x] End-to-end tests pass in local harness for v0 replacement gates.",
    "[x] Performance baseline work is classified as production-release follow-up, not a runtime replacement blocker today.",
    "[x] Security/release checklist is complete enough to hand off to SDK/build planning for v0.",
]
closure_evidence = [
    "Added `layer/surface/build/product/RELEASE-CHECKLIST-v0.md` with final tier-0 gate, runtime security gates, operator gates, and deferred production release work.",
    "Local validation before final tier-0: `cargo test --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` passed after binding signatures, secrets toy, JVM bridge, and docs.",
    "Production packaging/performance baselines remain deferred release follow-up rather than Mother runtime replacement blockers.",
]
blocked_by = []
implementation_plan = [
    "[x] Define end-to-end scenarios covering local daemon, Iroh peer call, process child, toy grant, observation log, and bridge basics.",
    "[x] Defer performance baselines for hot kernel decisions and envelope routing to production release work.",
    "[x] Review security boundaries for node auth, child auth, grants, secrets, and transport admission in the v0 checklist.",
    "[x] Prepare docs, packaging, and release checklist for MCT v0.",
]
created_at = "1780068572"
updated_at = "1780262310"
defer_reason = "Runtime replacement v0 is ready after final tier-0; broader production release hardening/perf baselines are follow-up work."
