id = "mct-slice-kernel-domain-records"
title = "Implement initial MCT kernel domain records"
kind = "build"
status = "complete"
human_request = "Add typed MCT kernel records for calls, results, peer binding, hello/call protocols, and observations."
execution_granularity = "slice"
allium_anchors = ["MctCall", "MctResult", "MctObservation", "MctPeerBinding", "MctHelloProtocol", "MctCallProtocol"]
user_alignment = "User wanted a greenfield MCT design where implementation traces to Allium and where Iroh transport identity is not runtime authority."
belief_refs = ["iroh-endpointid-is-transport-identity", "iroh-provides-connectivity-not-authority", "mct-call-protocol-wraps-semantic-call"]
core_refs = ["layer/core/mct-build-boundaries.md", "layer/core/spec-driven-design.md", "layer/core/dependable-rust.md", "layer/core/safety-boundaries.md"]
parent_slates = ["mct-kernel-crate", "mct-call-envelope", "mct-observation-log", "mct-node-identity-security", "mct-iroh-substrate"]
code_targets = ["crates/mct-kernel/src/id.rs", "crates/mct-kernel/src/call/mod.rs", "crates/mct-kernel/src/observation.rs", "crates/mct-kernel/src/peer/mod.rs", "crates/mct-kernel/src/lib.rs"]
non_goals = ["No routing engine implementation.", "No toy grant authorization engine.", "No storage schema or transport adapter effects."]
implementation_plan = ["[x] Implement typed string IDs.", "[x] Implement `MctCall` and `MctResult` records.", "[x] Implement peer binding, hello, call protocol, and observation records.", "[x] Add serialization/unit tests for stable edge representation."]
proof_plan = ["[x] Typed IDs roundtrip as strings.", "[x] `MctCall` roundtrips through JSON.", "[x] Iroh `EndpointId` appears only as transport text, not as authority.", "[x] Kernel public API exposes domain records without Iroh/Wasmtime/storage internals.", "[x] `cargo test --workspace` passes."]
closure_evidence = ["[x] Commit `b3512ac feat(kernel): add mct domain records`.", "[x] Later full validation also passed in `./scripts/ci-tier0.sh` after commit `a35b598`."]
proof_commands = ["cargo test --workspace", "allium check layer/allium", "./scripts/ci-tier0.sh"]
blocked_by = []
created_at = "1780262176"
updated_at = "1780262176"
closed_at = "1780262176"
