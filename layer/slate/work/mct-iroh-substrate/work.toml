id = "mct-iroh-substrate"
title = "Build production-shaped Iroh v0 substrate"
kind = "build"
status = "blocked"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Implement Iroh as a real early Mother-to-Mother substrate: persistent node identity, endpoint lifecycle, relay/discovery, peer admission, ALPN protocol, direct streams, observations, and multi-node tests."
allium_anchors = ["MctPeerBinding", "MctPeerAdmissionDecision", "MctIrohPeerBindingAuthority", "MctIrohProtocolPolicy", "MctIrohProtocolDeclaration", "MctIrohProtocolLayer", "MctHelloRequest", "MctHelloAdmissionEvaluation", "MctHelloResponse", "MctHelloProtocol", "MctCallProtocolRequest", "MctCallProtocolEvaluation", "MctCallProtocolReply", "MctCallProtocol", "MctRuntimeShape"]
user_alignment = "User rejected a skeleton Iroh path and pointed to the cached `n0-computer/iroh` repo as reference material. Later reframed MCT as a WASM/WASI/WIT app system over Iroh, with Iroh as the p2p substrate and MCT providing authority, observations, and thought-mesh semantics. User agreed that Iroh EndpointId is transport identity only; MCT requires explicit observed binding to Node/Vision/admission/policy authority. User-provided Iroh direction evidence reinforces that Iroh stops at connectivity and ALPN composition while MCT occupies the protocol/runtime/authority layer above."
belief_refs = ["mct-builds-on-iroh-substrate", "iroh-noq-evidence-before-rules", "iroh-endpointid-is-transport-identity", "iroh-provides-connectivity-not-authority", "mct-hello-precedes-protected-peer-effects", "mct-call-protocol-wraps-semantic-call"]
core_refs = ["layer/core/mct-build-boundaries.md", "layer/core/adapter-pattern.md", "layer/core/safety-boundaries.md"]
runtime_shape_role = "Peer connectivity adapter: owns Iroh endpoint/protocol effects while translating transport facts into MCT peer/call/observation records."
design_style = "Iroh-style protocol composition: ALPNs, explicit endpoint lifecycle, connection/stream handling, and application authority above transport identity."
implementation_ready_requirements = ["Every slice names the ALPN/protocol behavior it changes.", "No child receives raw Iroh endpoint/connection handles by default.", "Proof gates prove EndpointId alone is not MCT authority."]
slice_refs = ["mct-slice-kernel-domain-records", "mct-slice-hello-admission", "mct-slice-call-protocol-evaluation", "mct-slice-local-iroh-roundtrip", "mct-slice-runtime-shape-anchors", "mct-slice-iroh-unknown-peer-denial", "mct-slice-iroh-mother-owned-endpoint-lifecycle", "mct-slice-iroh-adapter-observations", "mct-slice-peer-binding-revocation-expiry", "mct-slice-iroh-ticket-peer-connection", "mct-slice-daemon-durable-config-approval-store", "mct-slice-iroh-serve-process-runtime-routing"]
partial_evidence = ["[x] Iroh/noq evidence has been captured before rule finalization.", "[x] Local relay-disabled Iroh endpoints complete `mct/hello/0` then `mct/call/0`.", "[x] Adapter proof keeps MCT authority in explicit `MctPeerBinding` and kernel evaluation.", "[x] Unknown-peer local Iroh integration denies `mct/hello/0` with no active `MctPeerBinding` and denies subsequent `mct/call/0` routing.", "[x] Mother-owned local Iroh endpoint lifecycle owner binds MCT ALPNs, exposes safe transport snapshots, and closes idempotently.", "[x] Iroh adapter can project endpoint lifecycle and peer protocol events into `MctObservation` facts.", "[x] Peer binding revocation/expiry authority behavior is locked in kernel tests before broader Iroh lifecycle work.", "[x] Manual endpoint-ticket peer connection works across two local MCT identities with explicit binding, admitted hello, and successful call reply.", "[x] Project-local peer address book persists endpoint tickets/bindings and `iroh call-peer` uses it.", "[x] `serve_next_with_call_handler` lets daemon route accepted Iroh calls through runtime execution before reply.", "[ ] Relay/discovery hardening and adapter observation persistence remain open."]
next_slice_candidates = []
proof_plan = [
    "[x] Local multi-node integration test completes mct/hello/0 admission before sending an MCT call envelope over Iroh and receiving a reply.",
    "[x] Peer admission denies unknown/untrusted peers and records a kernel observation.",
    "[x] A successful Iroh handshake or discovered EndpointId is insufficient without an active MctPeerBinding for the requested MCT ALPN/Vision scope.",
    "[x] Adapter observations capture endpoint and stream lifecycle without polluting kernel API with Iroh types.",
]
closure_evidence = []
blocked_by = [
    "mct-node-identity-security",
]
implementation_plan = [
    "[x] Study `/Users/nicabar/.patina/cache/repos/n0-computer/iroh` and relevant companion protocol repos before finalizing scope; see `layer/surface/iroh-substrate-evidence.md`.",
    "[x] Implement Mother-owned Iroh endpoint lifecycle rather than exposing raw Iroh handles to WASM/WASI/WIT children.",
    "[x] Add first persistent endpoint identity file support and typed endpoint-id derivation; full NodeId mapping store remains future work.",
    "[x] Implement endpoint lifecycle, relay-default config, address book, peer admission, and MCT ALPN request/reply as adapter behavior, not as the MCT authority model.",
    "[x] Define mct/hello/0 as the first MCT-over-Iroh ALPN for admission/version/capability exchange, treating ALPN as the protocol composition seam above Iroh connectivity; see MctHelloProtocol in Allium.",
    "[x] Define mct/call/0 for WIT-shaped remote calls after mct/hello/0 admission is accepted; see MctCallProtocol in Allium.",
    "[x] Implement direct bidirectional request/reply streams carrying the MCT call envelope for manual endpoint-ticket peers.",
    "[x] Emit first adapter observations for endpoint lifecycle and peer protocol receive/reply; relay fallback/stream reset/close persistence remains future work.",
]
created_at = "1780068505"
updated_at = "1781008973"
block_reason = "Epic remains open for relay/discovery hardening and persisted adapter observations, but address book, endpoint-ticket calls, unknown-peer denial, and runtime-routed serve path are complete."
