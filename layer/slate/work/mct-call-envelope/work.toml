id = "mct-call-envelope"
title = "Lock and implement MCT call envelope v1"
kind = "build"
status = "complete"
human_request = "Define and implement the stable MCT semantic call envelope shared by process harnesses, JVM bridge, WASM components, local control, and Iroh peer calls."
allium_anchors = [
    "MctCall",
    "MctCallProtocolAuthority",
    "MctCallPayloadHandle",
    "MctCallProtocolRequest",
    "MctCallProtocolEvaluation",
    "MctCallProtocolReply",
    "MctCallProtocol",
    "MctRuntimeShape",
]
user_alignment = "User agreed we need one stable envelope and asked to lock the shape rather than leave it vague. The Iroh call protocol is now specified as mct/call/0: a transport envelope around the semantic MctCall, admitted only after mct/hello/0."
belief_refs = ["mct-call-protocol-wraps-semantic-call"]
proof_plan = [
    "[x] Roundtrip tests cover JSON edge encoding, mct/call/0 edge encoding, and typed kernel representation.",
    "[x] Envelope supports caller/target/operation/payload/auth/deadline/idempotency/trace without transport-specific authority types.",
    "[x] Existing Mother `ChildCallRequest { operation_id, args, correlation }` is explicitly compared and narrowed/improved.",
]
closure_evidence = [
    "[x] `MctCall` and `MctResult` typed kernel records exist and roundtrip as JSON without becoming adapter-specific wire truth.",
    "[x] `MctCallProtocolRequest`, `MctCallProtocolEvaluation`, and `MctCallProtocolReply` implement the `mct/call/0` peer envelope around one semantic `MctCall`.",
    "[x] `call_protocol_json_edge_roundtrips_and_rejects_malformed` proves request/reply JSON edge roundtrip and malformed rejection.",
    "[x] `call_envelope_roundtrip_preserves_semantic_call_across_edges` proves typed `MctCall`, JSON edge encoding, `mct/call/0` evaluation, and reply linkage preserve the same semantic call.",
    "[x] `adapter-mapping.md` documents how Iroh, daemon control, CLI, process, JVM, and WASM/WIT edges translate into `MctCall`/`MctResult` without creating adapter-specific authority.",
]
blocked_by = []
implementation_plan = [
    "[x] Turn the `MctCallEnvelope v1` design shape into concrete typed Rust types.",
    "[x] Define caller, target, operation, payload handle, presented peer authority, deadline, idempotency, and trace fields for mct/call/0 in Allium.",
    "[x] Keep JSON edge conversions explicit without making JSON the kernel truth.",
    "[x] Document how process, JVM, WASM, UDS/HTTP, CLI, and Iroh adapters map into the envelope, using mct/call/0 as the remote peer mapping.",
    "[x] Leave concrete daemon control transport conversion to `mct-daemon-control-plane` child slices rather than expanding the call-envelope epic.",
]
belief_harvest_decision = "No new durable belief required; completed child slices apply existing `mct-call-protocol-wraps-semantic-call` and adapter-boundary beliefs to close the call envelope v1 epic."
created_at = "1780068477"
updated_at = "1780282956"
closed_at = "1780282956"
