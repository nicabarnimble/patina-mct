id = "mct-call-envelope"
title = "Lock and implement MCT call envelope v1"
kind = "build"
status = "blocked"
human_request = "Define and implement the stable MCT semantic call envelope shared by process harnesses, JVM bridge, WASM components, local control, and Iroh peer calls."
allium_anchors = ["MctCall", "MctCallProtocolAuthority", "MctCallPayloadHandle", "MctCallProtocolRequest", "MctCallProtocolEvaluation", "MctCallProtocolReply", "MctCallProtocol"]
user_alignment = "User agreed we need one stable envelope and asked to lock the shape rather than leave it vague. The Iroh call protocol is now specified as mct/call/0: a transport envelope around the semantic MctCall, admitted only after mct/hello/0."
belief_refs = ["mct-call-protocol-wraps-semantic-call"]
proof_plan = [
    "[ ] Roundtrip tests cover JSON edge encoding, mct/call/0 edge encoding, and typed kernel representation.",
    "[ ] Envelope supports caller/target/operation/payload/auth/deadline/idempotency/trace without transport-specific authority types.",
    "[ ] Existing Mother `ChildCallRequest { operation_id, args, correlation }` is explicitly compared and narrowed/improved.",
]
closure_evidence = []
blocked_by = [
    "mct-allium-foundation",
    "mother-kernel-design",
]
implementation_plan = [
    "[ ] Turn the `MctCallEnvelope v1` design shape into concrete typed Rust types.",
    "[x] Define caller, target, operation, payload handle, presented peer authority, deadline, idempotency, and trace fields for mct/call/0 in Allium.",
    "[ ] Add edge conversions from JSON/control-plane inputs without making JSON the kernel truth.",
    "[ ] Document how process, JVM, WASM, UDS/HTTP, and Iroh adapters map into the envelope, using mct/call/0 as the remote peer mapping.",
]
created_at = "1780068477"
updated_at = "1780070118"
block_reason = "Blocked until mother-kernel-design is completed or explicitly superseded in the roadmap."
