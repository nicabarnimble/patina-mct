id = "mct-verified-child-approval"
title = "Require verified child package integrity before approval"
kind = "build"
status = "completed"
execution_granularity = "slice"
human_request = "Finish the Slate/WIT runtime path by making bad approved-but-unexecutable child states impossible where practical."
allium_anchors = ["KernelDecidesAdaptersPerform", "ObservationsAreCanonicalRuntimeTruth", "DaemonOwnsLifecycleButNotAuthority"]
user_alignment = "User asked to favor strong invariants, explicit contracts, and scalpel commits."
belief_refs = ["mother-kernel-decides-adapters-perform", "typed-domain-records-before-algorithms"]
code_targets = ["crates/mct-daemon/src/config.rs", "crates/mct-daemon/src/children.rs"]
implementation_ready = true
runtime_shape_role = "A child cannot be stored as approved/assigned unless its manifest and wasm artifact hashes are verified up front."
non_goals = ["No fallback approval for unverified local dev artifacts.", "No CLI flag that mints trust after loading an unverified package."]
implementation_plan = ["[x] Add a shared loaded-child integrity predicate.", "[x] Reuse the predicate for artifact projection verification status.", "[x] Reject `approve_and_assign_loaded_child` before mutating config when package integrity is unverified.", "[x] Add a config-store regression test proving no approval is persisted for an unverified child."]
proof_plan = ["[x] cargo test -p mct-daemon config::tests::config_store"]
closure_evidence = ["`MctLoadedChild::integrity_verified` centralizes the wasm+manifest digest invariant.", "Config approval now fails before writing config if either digest is unverified.", "Regression test proves failed approval leaves the config without child approvals."]
blocked_by = []
created_at = "1782929000"
updated_at = "1782929000"
