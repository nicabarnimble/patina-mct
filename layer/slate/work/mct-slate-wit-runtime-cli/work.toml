id = "mct-slate-wit-runtime-cli"
title = "Wire Slate typed WIT execution through MCT runtime CLI"
kind = "build"
status = "active"
execution_granularity = "slice"
human_request = "Finish the Slate/WIT runtime path by wiring explicit WASI preopens and authorized toy adapters into a real MCT invocation surface."
allium_anchors = ["KernelDecidesAdaptersPerform", "ObservationsAreCanonicalRuntimeTruth", "DaemonOwnsLifecycleButNotAuthority"]
user_alignment = "User asked to finish the Slate/WIT runtime work with strong invariants, explicit contracts, and scalpel commits."
belief_refs = ["mother-kernel-decides-adapters-perform", "typed-domain-records-before-algorithms"]
code_targets = ["crates/mct-daemon/src/main.rs", "crates/mct-daemon/src/wasm.rs", "crates/mct-daemon/src/state.rs"]
implementation_ready = true
runtime_shape_role = "The daemon can launch an approved Slate-like WIT child with explicit project-root preopen authority and authorized logging/measure/git toy adapters."
non_goals = ["No generic WIT host import stubs.", "No ambient filesystem inheritance.", "No implicit toy authority from CLI flags.", "No Wasmtime/WASI types in mct-kernel."]
implementation_plan = ["[ ] Add a typed WIT CLI invocation path that loads an approved child package instead of taking a raw component file.", "[ ] Build WIT host adapters from persisted toy catalog/grant snapshots and explicit project-root preopen input.", "[ ] Add an explicit operator command to seed the narrow Slate local runtime grants for an approved child.", "[ ] Validate actual Slate list-work execution under MCT."]
proof_plan = ["[ ] cargo test -p mct-daemon mct_wit_runtime", "[ ] cargo clippy -p mct-daemon --lib -- -D warnings", "[ ] cargo test -p mct-kernel -p mct-daemon", "[ ] ./scripts/ci-tier0.sh", "[ ] run Slate artifact through mct-daemon wasm call-wit"]
closure_evidence = []
blocked_by = []
created_at = "1782926270"
updated_at = "1782926270"
