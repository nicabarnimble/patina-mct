id = "mct-child-package-install"
title = "Install verified child packages into the MCT registry"
kind = "build"
status = "completed"
execution_granularity = "slice"
human_request = "Move MCT toward the child registry replacement threshold with strong package integrity invariants and no implicit approval."
allium_anchors = ["KernelDecidesAdaptersPerform", "ObservationsAreCanonicalRuntimeTruth", "DaemonOwnsLifecycleButNotAuthority"]
user_alignment = "User asked to finish the MCT replacement thresholds while favoring strong invariants, simple explicit contracts, and scalpel commits."
belief_refs = ["mother-kernel-decides-adapters-perform", "typed-domain-records-before-algorithms"]
code_targets = ["crates/mct-daemon/src/registry.rs", "crates/mct-daemon/src/children.rs", "crates/mct-daemon/src/main.rs"]
implementation_ready = true
runtime_shape_role = "MCT can install a strictly verified SDK child package into the local children registry without implicitly approving or granting it."
non_goals = ["No network downloader yet.", "No implicit child approval from install.", "No implicit toy grants from install.", "No guessing artifact identity from directory contents."]
implementation_plan = ["[x] Load SDK child packages from child subdirectories under the children registry root.", "[x] Add an atomic verified package install operation that preserves package-relative manifest artifact paths and sidecars.", "[x] Reject unverified package installs and existing installs unless `--replace` is explicit.", "[x] Add `mct-daemon registry install` CLI wiring."]
proof_plan = ["[x] cargo test -p mct-daemon children::tests::loads_sdk_child_packages_from_child_subdirectories", "[x] cargo test -p mct-daemon registry::tests::"]
closure_evidence = ["`load_children_from_dir` now discovers SDK child package subdirectories containing child.toml.", "`install_verified_child_package` stages verified package files, preserves the declared artifact path, and atomically renames into `<children-dir>/<child-name>`.", "Install copies only installable package facts: child.toml, child.toml.sha256, wasm artifact, wasm sidecar, and optional checksums.txt.", "Existing child package replacement requires explicit `--replace`.", "Install does not approve the child or mint toy grants."]
blocked_by = []
created_at = "1782931200"
updated_at = "1782931200"
