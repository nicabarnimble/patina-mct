id = "mct-process-harness"
title = "Build process child harness"
kind = "build"
status = "blocked"
human_request = "Implement the process/executable child runtime adapter to prove the child protocol before full JVM and WASM runtime depth."
allium_anchors = []
user_alignment = "Process harness is the simplest proof path for lifecycle, routing, envelope, observations, and adapter boundaries without over-engineering."
belief_refs = []
core_refs = ["layer/core/adapter-pattern.md", "layer/core/dependable-rust.md", "layer/core/safety-boundaries.md"]
proof_plan = [
    "[ ] Echo process child can be installed/activated/called/stopped through the daemon.",
    "[ ] Timeout and crash cases return typed adapter diagnostics and kernel observations stay linked by call_id.",
    "[ ] Process runtime remains an adapter outside `mct-kernel`.",
]
closure_evidence = []
blocked_by = [
    "mct-child-registry-lifecycle",
    "mct-call-envelope",
    "mct-observation-log",
]
implementation_plan = [
    "[ ] Define process child manifest shape and stdin/stdout or socket protocol mapping to MCT call envelope.",
    "[ ] Implement spawn, health, call, timeout, stop, and exit handling.",
    "[ ] Emit adapter observations for spawn/exit/stdout/stderr/timeout/failure.",
    "[ ] Add an echo/test child fixture.",
]
created_at = "1780068518"
updated_at = "1780070132"
block_reason = "Blocked until mct-observation-log is completed or explicitly superseded in the roadmap."
