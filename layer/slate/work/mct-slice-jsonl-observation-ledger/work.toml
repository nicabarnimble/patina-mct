id = "mct-slice-jsonl-observation-ledger"
title = "Implement append-only JSONL observation ledger"
kind = "build"
status = "complete"
human_request = "Add the first concrete append-only observation ledger so `MctObservation` is canonical runtime truth and logs/metrics remain projections."
execution_granularity = "slice"
allium_anchors = ["MctObservation"]
user_alignment = "User stated that the `MctObservation` ledger is canonical runtime truth and external observability is projection."
belief_refs = ["mother-kernel-decides-adapters-perform"]
core_refs = ["layer/core/oxidized-knowledge.md", "layer/core/safety-boundaries.md", "layer/core/mct-build-boundaries.md", "layer/core/dependable-rust.md"]
parent_slates = ["mct-observation-log", "mct-kernel-crate", "mct-daemon-control-plane", "mct-iroh-substrate"]
code_targets = ["crates/mct-observation/src/lib.rs", "crates/mct-observation/Cargo.toml"]
non_goals = ["No SQLite/storage abstraction.", "No OpenTelemetry/exporter implementation.", "No batching policy beyond concrete append-before-effect semantics."]
implementation_plan = ["[x] Implement JSONL ledger entries around `MctObservation`.", "[x] Append before effect and maintain hash-chain links.", "[x] Validate hash chain when reopening an existing ledger.", "[x] Add trace and call query helpers.", "[x] Fail closed on IO and JSON corruption."]
proof_plan = ["[x] Append and read roundtrip test passes.", "[x] Queries by trace and call return expected entries.", "[x] Reopening validates existing hash chain.", "[x] Opening a directory fails closed.", "[x] `cargo test -p mct-observation` passes."]
closure_evidence = ["[x] Commit `b7f9246 feat(observation): add append-only jsonl ledger`.", "[x] Later full validation also passed in `./scripts/ci-tier0.sh` after commit `a35b598`."]
proof_commands = ["cargo test -p mct-observation", "allium check layer/allium", "./scripts/ci-tier0.sh"]
blocked_by = []
created_at = "1780262176"
updated_at = "1780262176"
closed_at = "1780262176"
