id = "mct-authoritative-slate-local"
title = "Expose local Slate execution through MCT"
kind = "build"
status = "completed"
execution_granularity = "slice"
human_request = "Make MCT the local authoritative Slate runtime path rather than requiring operators to spell raw WIT invocation details."
allium_anchors = ["KernelDecidesAdaptersPerform", "ObservationsAreCanonicalRuntimeTruth", "DaemonOwnsLifecycleButNotAuthority"]
user_alignment = "User wants the Slate runtime threshold: MCT can run Slate locally as the authoritative Slate child and old Mother is not needed for local Slate work."
belief_refs = ["mother-kernel-decides-adapters-perform", "typed-domain-records-before-algorithms"]
code_targets = ["crates/mct-daemon/src/main.rs"]
implementation_ready = true
runtime_shape_role = "Operators can invoke Slate list-work through an MCT Slate command backed by the installed/approved Slate WIT child."
non_goals = ["No authority minted by the Slate convenience command.", "No fallback to old Mother.", "No extra WIT adapter path separate from the package-backed call-wit path."]
implementation_plan = ["[x] Add `mct-daemon slate list-work` as an MCT-native Slate surface.", "[x] Reuse the existing package-backed `wasm call-wit` path rather than adding a parallel runtime path.", "[x] Require explicit `--project-root` so `/project` remains an explicit preopen input."]
proof_plan = ["[x] cargo check -p mct-daemon", "[x] install Slate release package, approve it, authorize Slate toys, and run `mct-daemon slate list-work`"]
closure_evidence = ["`mct-daemon slate list-work` invokes `patina:slate/control@0.1.0.list-work` through the same child approval, toy grant, WIT adapter, and WASI preopen checks as `wasm call-wit`.", "The convenience command constructs only the typed Slate request and operation id; it does not approve children or authorize toys.", "End-to-end smoke passed using `registry install`, strict `children approve`, `toys authorize-slate`, and `slate list-work`; result outcome was success with real Slate rows."]
blocked_by = []
created_at = "1782932200"
updated_at = "1782932200"
