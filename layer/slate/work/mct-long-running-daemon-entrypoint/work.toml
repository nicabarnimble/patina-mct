id = "mct-long-running-daemon-entrypoint"
title = "Add a long-running MCT daemon serve entrypoint"
kind = "build"
status = "completed"
execution_granularity = "slice"
human_request = "Move MCT toward the daemon replacement threshold with a clear long-running local daemon command and observable control surface."
allium_anchors = ["DaemonOwnsLifecycleButNotAuthority", "ObservationsAreCanonicalRuntimeTruth", "KernelDecidesAdaptersPerform"]
user_alignment = "User wants MCT to run as a long-lived local daemon with observable health/control without adding unclear abstractions."
belief_refs = ["mother-kernel-decides-adapters-perform", "typed-domain-records-before-algorithms"]
code_targets = ["crates/mct-daemon/src/main.rs"]
implementation_ready = true
runtime_shape_role = "Operators can start a long-running local MCT daemon control loop through `mct-daemon serve`."
non_goals = ["No hidden auto-approval or auto-grants on daemon start.", "No new daemon supervisor abstraction.", "No fallback to old Mother."]
implementation_plan = ["[x] Add top-level `mct-daemon serve` command.", "[x] Reuse the existing HTTP/UDS control handlers and state-backed snapshot logic.", "[x] Keep transport selection explicit with `--http` or `--uds`; default to local HTTP for cross-platform behavior."]
proof_plan = ["[x] cargo check -p mct-daemon"]
closure_evidence = ["`mct-daemon serve` starts a long-running control loop backed by `control_snapshot`.", "Existing `control serve-http` and `control serve-uds` commands now share the same HTTP/UDS serve helpers.", "The daemon entrypoint exposes health/state/runs through the existing control surface and does not mutate child or toy authority."]
blocked_by = []
created_at = "1782933000"
updated_at = "1782933000"
