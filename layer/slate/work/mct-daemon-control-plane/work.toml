id = "mct-daemon-control-plane"
title = "Build MCT daemon local control plane"
kind = "build"
status = "blocked"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Build the first clean MCT daemon shell with local control-plane transport, health/version/readiness, lifecycle wiring, and kernel integration."
allium_anchors = ["MctRuntimeShape"]
user_alignment = "MCT needs a daemon, but the clean repo should not inherit current Mother's broad ServerState/ApiRuntime coupling."
belief_refs = []
core_refs = ["layer/core/unix-philosophy.md", "layer/core/dependable-rust.md", "layer/core/safety-boundaries.md", "layer/core/mct-build-boundaries.md"]
runtime_shape_role = "Lifecycle/control owner: owns daemon process, local control surface, endpoint/runtime wiring, and readiness without becoming the authorization truth."
design_style = "Small orchestration layer over concrete crates; avoid one broad ServerState clone from current Mother."
implementation_ready_requirements = ["Every slice names which lifecycle/control boundary it changes.", "Kernel decisions remain in mct-kernel and are not reimplemented in handlers.", "Readiness/health proof gates distinguish daemon availability from authority admission."]
slice_refs = ["mct-slice-workspace-skeleton", "mct-slice-fake-daemon-echo", "mct-slice-runtime-shape-anchors", "mct-slice-iroh-mother-owned-endpoint-lifecycle"]
partial_evidence = ["[x] Daemon crate exists.", "[x] Fake vertical slice composes kernel evaluation, echo result, and observation ledger.", "[x] Iroh endpoint lifecycle now has a daemon-owned wrapper shape rather than raw endpoint ownership leaking outward.", "[ ] Local UDS/HTTP control plane, health/version/readiness, and authorization gates remain open."]
next_slice_candidates = ["mct-slice-daemon-health-readiness", "mct-slice-daemon-local-control-api"]
proof_plan = [
    "[ ] Daemon starts locally and reports health/version/readiness.",
    "[ ] Control-plane tests prove unauthenticated/unauthorized paths fail closed where applicable.",
    "[ ] Daemon state is smaller and more domain-split than current integrated `ServerState`.",
]
closure_evidence = []
blocked_by = [
    "mct-kernel-crate",
    "mct-storage-core",
    "mct-observation-log",
]
implementation_plan = [
    "[ ] Create daemon bootstrap with UDS/local control-plane first and optional HTTP placement documented.",
    "[ ] Expose health, version, readiness, and basic status endpoints/commands.",
    "[ ] Wire daemon state to `mct-kernel` without leaking transport concerns into the kernel.",
    "[ ] Add graceful start/stop, logging, and readiness tests.",
]
created_at = "1780068498"
updated_at = "1780275603"
block_reason = "Epic remains open; fake daemon vertical slice is complete, but real local control plane and readiness behavior still require smaller slice Slates."
