id = "mct-daemon-control-plane"
title = "Build MCT daemon local control plane"
kind = "build"
status = "blocked"
execution_granularity = "epic_container"
direct_implementation_rule = "Do not implement or close this whole Slate in one large change. Create or use child slice Slates with named code targets, non-goals, binary proof gates, commit evidence, and validation commands."
completion_rule = "Complete this epic only when slice_refs and closure_evidence cover every proof gate with committed implementation and passing validation."
human_request = "Build the first clean MCT daemon shell with local control-plane transport, health/version/readiness, lifecycle wiring, and kernel integration."
allium_anchors = ["MctRuntimeShape"]
user_alignment = "MCT needs a daemon, but the clean repo should not inherit current Mother's broad ServerState/ApiRuntime coupling."
belief_refs = []
core_refs = ["layer/core/unix-philosophy.md", "layer/core/dependable-rust.md", "layer/core/safety-boundaries.md", "layer/core/mct-build-boundaries.md"]
runtime_shape_role = "Lifecycle/control owner: owns daemon process, local control surface, endpoint/runtime wiring, and readiness without becoming the authorization truth."
design_style = "Small orchestration layer over concrete crates; avoid one broad ServerState clone from current Mother."
implementation_ready_requirements = ["Every slice names which lifecycle/control boundary it changes.", "Kernel decisions remain in mct-kernel and are not reimplemented in handlers.", "Readiness/health proof gates distinguish daemon availability from authority admission."]
slice_refs = ["mct-slice-workspace-skeleton", "mct-slice-fake-daemon-echo", "mct-slice-runtime-shape-anchors", "mct-slice-iroh-mother-owned-endpoint-lifecycle", "mct-slice-daemon-health-readiness", "mct-slice-daemon-fake-end-to-end-status", "mct-slice-daemon-module-shape", "mct-slice-daemon-local-control-status-api", "mct-slice-daemon-child-directory-loader", "mct-slice-iroh-ticket-peer-connection", "mct-slice-daemon-child-authority-projection", "mct-slice-daemon-durable-config-approval-store", "mct-slice-process-child-authorized-echo", "mct-slice-process-supervisor-lifecycle", "mct-slice-wasm-component-authorized-s32", "mct-slice-iroh-serve-process-runtime-routing"]
partial_evidence = ["[x] Daemon crate exists.", "[x] Fake vertical slice composes kernel evaluation, echo result, and observation ledger.", "[x] Iroh endpoint lifecycle now has a daemon-owned wrapper shape rather than raw endpoint ownership leaking outward.", "[x] Daemon health/readiness status distinguishes process health from Iroh adapter readiness.", "[x] Fake end-to-end status composes daemon readiness, fake echo result, and ledger observation counts without a broad control API.", "[x] Daemon module shape keeps production status API separate from test-only fake proof harness.", "[x] Typed in-process local control status handler returns daemon status without adding HTTP/UDS transport or daemon authority logic.", "[x] CLI can load standalone child artifacts from project-local `.mct/children` by default and inspect explicit fixture directories when provided.", "[x] Daemon can project loaded children into kernel authority records and expose local candidates only after explicit verified local activation.", "[x] CLI can persist child approval/revocation and peer address-book config under `.mct/config.json`.", "[x] CLI can invoke authorized one-shot process children and minimal authorized WASM components.", "[x] CLI can create Iroh identities, serve an explicit peer binding, serve-process through runtime execution, call a peer endpoint ticket, and call a configured peer.", "[ ] Local UDS/HTTP transport and multi-operator control authorization gates remain open."]
next_slice_candidates = ["mct-slice-daemon-local-control-transport", "mct-slice-daemon-control-auth-denial"]
proof_plan = [
    "[ ] Daemon starts locally and reports health/version/readiness.",
    "[ ] Control-plane tests prove unauthenticated/unauthorized paths fail closed where applicable.",
    "[ ] Daemon state is smaller and more domain-split than current integrated `ServerState`.",
]
closure_evidence = []
blocked_by = [
    "mct-storage-core",
]
implementation_plan = [
    "[ ] Create daemon bootstrap with UDS/local control-plane first and optional HTTP placement documented.",
    "[x] Expose health, version, readiness, and basic status through a typed in-process control handler.",
    "[ ] Add UDS/HTTP transport around the typed control handler.",
    "[ ] Wire daemon state to `mct-kernel` without leaking transport concerns into the kernel.",
    "[ ] Add graceful start/stop, logging, and readiness tests.",
]
created_at = "1780068498"
updated_at = "1781008973"
block_reason = "Epic remains open; fake daemon vertical slice is complete, but real local control plane and readiness behavior still require smaller slice Slates."
