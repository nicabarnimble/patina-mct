id = "mct-slice-local-iroh-roundtrip"
title = "Prove local Iroh mct/hello then mct/call roundtrip"
kind = "build"
status = "complete"
human_request = "Prove real local Iroh endpoints can carry `mct/hello/0` then `mct/call/0` while MCT kernel authority remains above transport."
execution_granularity = "slice"
allium_anchors = ["MctPeerBinding", "MctIrohPeerBindingAuthority", "MctHelloProtocol", "MctCallProtocol"]
user_alignment = "User wanted baseline MCT-over-Iroh before ECH/OHTTP or broader substrate work, with Mother owning Iroh endpoint lifecycle and MCT providing authority."
belief_refs = ["mct-builds-on-iroh-substrate", "iroh-noq-evidence-before-rules", "iroh-endpointid-is-transport-identity", "iroh-provides-connectivity-not-authority", "mct-hello-precedes-protected-peer-effects", "mct-call-protocol-wraps-semantic-call"]
core_refs = ["layer/core/adapter-pattern.md", "layer/core/safety-boundaries.md", "layer/core/mct-build-boundaries.md", "layer/core/spec-driven-design.md"]
parent_slates = ["mct-iroh-substrate", "mct-call-envelope", "mct-node-identity-security", "mct-observation-log"]
code_targets = ["crates/mct-iroh/src/lib.rs", "crates/mct-iroh/Cargo.toml", "Cargo.toml", "Cargo.lock"]
non_goals = ["No production endpoint lifecycle or persistent node key store.", "No relay/discovery/address-book configuration.", "No adapter observation export.", "No raw Iroh handles exposed to children."]
implementation_plan = ["[x] Add real local relay-disabled Iroh server and client endpoints.", "[x] Negotiate `mct/hello/0`, evaluate explicit `MctPeerBinding`, and return safe hello response.", "[x] Negotiate `mct/call/0` only after admitted hello and return safe call reply.", "[x] Add async integration test for the roundtrip."]
proof_plan = ["[x] Local Iroh endpoints complete `mct/hello/0` before `mct/call/0`.", "[x] Server evaluates kernel authority from explicit `MctPeerBinding`, not EndpointId alone.", "[x] Call reply succeeds only after admitted hello.", "[x] `cargo test -p mct-iroh local_iroh_completes_mct_hello_then_call` passes.", "[x] `cargo test --workspace` passes."]
closure_evidence = ["[x] Commit `a35b598 feat(iroh): prove local mct protocol roundtrip`.", "[x] `./scripts/ci-tier0.sh` passed with all workspace tests and Allium validation."]
proof_commands = ["cargo test -p mct-iroh local_iroh_completes_mct_hello_then_call", "cargo test --workspace", "allium check layer/allium", "./scripts/ci-tier0.sh"]
blocked_by = []
created_at = "1780262176"
updated_at = "1780262176"
closed_at = "1780262176"
