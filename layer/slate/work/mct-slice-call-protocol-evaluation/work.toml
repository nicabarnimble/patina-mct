id = "mct-slice-call-protocol-evaluation"
title = "Implement pure mct/call/0 protocol evaluation"
kind = "build"
status = "complete"
human_request = "Add pure kernel evaluation for `mct/call/0` as a transport envelope around the semantic `MctCall` after admitted hello."
execution_granularity = "slice"
allium_anchors = ["MctCall", "MctCallProtocolAuthority", "MctCallPayloadHandle", "MctCallProtocolRequest", "MctCallProtocolEvaluation", "MctCallProtocolReply", "MctCallProtocol"]
user_alignment = "User locked `mct/call/0` as a peer transport envelope, not a second call model, and required hello admission first."
belief_refs = ["mct-call-protocol-wraps-semantic-call", "mct-hello-precedes-protected-peer-effects", "iroh-endpointid-is-transport-identity"]
core_refs = ["layer/core/mct-build-boundaries.md", "layer/core/spec-driven-design.md", "layer/core/dependable-rust.md", "layer/core/safety-boundaries.md"]
parent_slates = ["mct-call-envelope", "mct-kernel-crate", "mct-iroh-substrate", "mct-observation-log"]
code_targets = ["crates/mct-kernel/src/call/mod.rs", "crates/mct-kernel/src/call/internal.rs"]
non_goals = ["No child routing/execution engine.", "No real network adapter.", "No payload storage/blob transfer implementation."]
implementation_plan = ["[x] Move call internals behind a black-box module boundary.", "[x] Implement `evaluate_call_protocol` using admitted hello, accepted ALPN, endpoint match, policy revisions, and payload metadata.", "[x] Implement caller-safe protocol reply construction.", "[x] Add deny/malformed/accepted unit tests."]
proof_plan = ["[x] Call without admitted hello is denied.", "[x] Hello without `mct/call/0` ALPN does not authorize call.", "[x] Endpoint mismatch is denied.", "[x] Payload metadata mismatch is malformed.", "[x] Admitted hello allows call for routing without pre-authorizing execution.", "[x] `cargo test -p mct-kernel` passes."]
closure_evidence = ["[x] Commit `103cfab feat(kernel): evaluate mct call protocol`.", "[x] Later full validation also passed in `./scripts/ci-tier0.sh` after commit `a35b598`."]
proof_commands = ["cargo test -p mct-kernel", "allium check layer/allium", "./scripts/ci-tier0.sh"]
blocked_by = []
created_at = "1780262176"
updated_at = "1780262176"
closed_at = "1780262176"
