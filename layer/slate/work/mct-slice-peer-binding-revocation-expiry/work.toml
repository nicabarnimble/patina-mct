id = "mct-slice-peer-binding-revocation-expiry"
title = "Lock peer binding revocation and expiry behavior"
kind = "build"
status = "active"
execution_granularity = "slice"
human_request = "Add tests and domain observation helpers for revoked and expired MctPeerBinding behavior."
allium_anchors = ["MctPeerBinding", "MctHelloAdmissionEvaluation", "MctObservation", "MctRuntimeShape"]
user_alignment = "User wants explicit MCT peer authority above Iroh EndpointId, including revocation and expiry as observable facts."
belief_refs = ["iroh-endpointid-is-transport-identity", "mct-hello-precedes-protected-peer-effects"]
core_refs = ["layer/core/safety-boundaries.md", "layer/core/oxidized-knowledge.md", "layer/core/spec-driven-design.md"]
parent_slates = ["mct-node-identity-security", "mct-observation-log", "mct-iroh-substrate"]
code_targets = ["crates/mct-kernel/src/peer/mod.rs", "crates/mct-kernel/src/observation.rs"]
implementation_ready = true
runtime_shape_role = "Revoked/expired peer authority is denied by kernel policy and represented as canonical observation facts."
design_style = "Pure kernel tests and mapping helpers; no persistent identity store yet."
first_test_to_write = "expired_binding_is_denied_with_safe_message"
exit_criteria = ["Expired binding denies hello with safe message.", "Revoked binding denies hello with safe message.", "Revoked and expired binding states map to observation facts."]
non_goals = ["No persistent key store.", "No administrative revocation command.", "No clock-based expiry evaluation beyond explicit binding state."]
implementation_plan = [
    "[ ] Add expired-binding denial test beside revoked-binding denial.",
    "[ ] Add pure binding-state observation mapper.",
    "[ ] Add tests for revoked and expired observation facts.",
]
proof_plan = [
    "[ ] `cargo test -p mct-kernel expired_binding_is_denied_with_safe_message` passes.",
    "[ ] `cargo test -p mct-kernel revoked_and_expired_bindings_become_observations` passes.",
    "[ ] Test proves revoked/expired bindings deny with caller-safe messages.",
    "[ ] Test proves revoked/expired bindings become `PeerBindingRevoked`/`PeerBindingExpired` observations.",
    "[ ] `cargo test --workspace` passes.",
    "[ ] `allium check layer/allium` passes.",
]
closure_evidence = []
blocked_by = ["mct-slice-hello-admission", "mct-slice-kernel-denial-observations"]
created_at = "1780276500"
updated_at = "1780276500"
